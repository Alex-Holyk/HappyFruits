import React from 'react';
import NumericInput from 'react-numeric-input';

const FruitItem = ({ item }) => {
  return (
    <div>
      {/* <NumericInput
        mobile
        min={0}
        value={basketProducts?.apple}
        onChange={(value) => setBasketProducts({ apple: value })}
      /> */}
    </div>
  );
};

export default FruitItem;
